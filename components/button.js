import React from 'react'
import { render } from 'react-dom'

class Button extends React.Component {
  render() {
      return(
    <button>Not in the mood for cake?</button>
   )
  }
}

export default Button
